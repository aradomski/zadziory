<% title "Register" %>
<div id="register">
<%= semantic_form_for(resource, :as => resource_name, :url => registration_path(resource_name)) do |f| %>
  <%= devise_error_messages! %>
	<%= f.inputs do %>
	
	<%= f.input :email, :as => :email, :input_html => { :size => 30 } %>

	<%= f.input :password, :as => :password, :input_html => { :size => 30 } %>

	<%= f.input :password_confirmation, :as => :password , :input_html => { :size => 30 } %>
	
	<div class="radio_roles">
		<%= render :partial => "devise/shared/roles"%>
	</div>

	<%end%>
	
	<span class="rr">
	<fieldset class="knob">
	<%= f.buttons do %>
		<%= f.commit_button :label => "Register" %>
	<% end %>
	</fieldset>
	</span>
	
<% end %>

<div class="shared_links">
	<%= render :partial => "devise/shared/links" %>
</div>
</div>

<aside class="social_logins">
	<p> Jeśli <b>nie jesteś właścicielem</b> i interesuje Cię tylko przeglądanie ofert, możesz błyskawicznie
		zalogować się z pomocą jednego z portali społecznościowych.</p>
	<a href="/auth/facebook"><%=image_tag("facebook_48.png", :alt=>"Login with Facebook", :class=>"imagedropshadow")%></a>
	<a href="/auth/twitter"><%=image_tag("twitter_48.png", :alt=>"Login with Twitter", :class=>"imagedropshadow")%></a>
</aside>
