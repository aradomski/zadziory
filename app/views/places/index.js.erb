// render _place.html.erb partial
$("#places").html("<%= escape_javascript(render(@places)) %>");
// modify pagination links
$('#paginator').html('<%= escape_javascript(paginate(@places, :remote => true).to_s) %>');
