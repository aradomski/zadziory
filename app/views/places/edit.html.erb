<%= t('place.edit.title') %>

<div class="form edit">
  <%= render 'form' %>
  <div class="link">
    <%= link_to t('place.edit.show'), @place, class: "btn primary" %>
    <%= link_to t('place.edit.back'), places_path, class: "btn primary" %>
  </div>
</div>
