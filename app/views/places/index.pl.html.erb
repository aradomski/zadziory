<% title "Wszystkie oferty" %>

	<div class="link">
	<% if can? :create, Place %>
		<%= link_to 'Nowa oferta', new_place_path, class: "btn primary" %>
	<% end %>
		 <%= link_to 'Pokaż wszystkie', places_path, :remote => true, class: "btn primary" %>
	</div>
	
<article class="index">

<div class="seearch">
	<%= render :partial => 'search' %>
</div>

<div id="places">
	<%= render :partial => 'place', :collection => @places %>
	
	
	<%= params[:search] %>
</div>

</article>




