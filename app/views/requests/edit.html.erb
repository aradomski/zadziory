<% title t('requests.edit.title') %>

<div class="form edit">
  <%= render 'form' %>
  <div class="link">
    <%= link_to t('place.edit.show'), @request, class: "btn primary" %>
    <%= link_to t('place.edit.back'), requests_path, class: "btn primary" %>
  </div>
</div>
